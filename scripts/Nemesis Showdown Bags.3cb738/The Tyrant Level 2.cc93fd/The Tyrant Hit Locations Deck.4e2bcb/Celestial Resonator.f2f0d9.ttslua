function criticalWoundEffect()
  Global.call('addBroadcast', {"crit", {gear = "If there are 5+ Gates ~ Gain the Frozen Star SFA", effect = ""}})
end

function woundEffect()
end

function failureEffect()

end

function reflexEffect()
	Global.call('addBroadcast', {"reflex", {gear = "Perform Resonate ~ Targeting the Attacker", effect = ""}})
end

function addStatModifier()
end
