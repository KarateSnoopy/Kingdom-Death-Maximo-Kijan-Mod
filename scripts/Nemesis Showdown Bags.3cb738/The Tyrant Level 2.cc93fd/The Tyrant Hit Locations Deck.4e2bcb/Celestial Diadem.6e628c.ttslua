function criticalWoundEffect()
  Global.call('addBroadcast', {"crit", {gear = "Gain Survival = to Number of Gates on Showdown", effect = ""}})
end

function woundEffect()
end

function failureEffect()

end

function reflexEffect()
	Global.call('addBroadcast', {"reflex", {gear = "Perform Crooked Step", effect = ""}})
end

function addStatModifier()
end
